import { ExtendedSermon } from "@/app/(dashboard)/_components/sermons";
import usePlayer from "./use-player";
import { useCurrentUser } from "./use-current-user";

const useOnPlay = (sermons: ExtendedSermon[]) => {};

export default useOnPlay;
